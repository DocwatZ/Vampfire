default: &default
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL", "redis://localhost:6379") %>

development:
  <<: *default
  channel_prefix: vampfire_development

test:
  adapter: test

performance:
  <<: *default
  channel_prefix: vampfire_performance

production:
  <<: *default
  channel_prefix: vampfire_production
